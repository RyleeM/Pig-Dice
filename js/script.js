var score=0;
var total=0;
var dice_number = 0;

while (true) {


var numRolled= Die.Next(1,6);

if (numRolled>1)
(
  //pass to other player


function pig(var score, var total, var dice_number)
{
  // while(dice_number>1)
  // {
  //     Random Die = new Random();
  //     var dice_number = Die.Next(1,6);
  //     score = score +
  // }
  // return pig()
 total = score+numRolled;
 //document.write(total);





}
function Roll()
{
  document,write(total);
}
